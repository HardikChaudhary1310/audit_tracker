DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Hardik@12345
DB_NAME=user_activity
NODE_ENV=development
# --- Email Configuration (Gmail) ---
# Your Gmail address for sending emails
GMAIL_USER=hardikchaudhary713@gmail.com
# Your Gmail App Password (generated in Google Account settings)
GMAIL_APP_PASSWORD=atxj ijbl xuxt gwfe
# --- Database Configuration ---
DATABASE_URL=mysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:3306/${DB_NAME}
# --- Environment Configuration ---
# Sets the environment type for local runs
NODE_ENV=development

# --- Server Configuration ---
# Port your LOCAL Node.js server will use
PORT=3001
# Base URL for links generated during LOCAL testing (e.g., email links)
# It points to your server running on your Mac.
BASE_URL=http://localhost:3001