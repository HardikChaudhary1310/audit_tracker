# --- Database Configuration (PostgreSQL) ---
# For Local Development (replace user/db if different):
# DATABASE_URL=postgres://YOUR_LOCAL_USERNAME@localhost:5432/YOUR_LOCAL_DB_NAME
# Example for user 'hardikchaudhary' and db 'user_activity_db':
DATABASE_URL=postgres://hardikchaudhary@localhost:5432/user_activity_db

# For Render Deployment:
# Render will inject DATABASE_URL automatically. DO NOT set it manually in Render's UI
# unless you are connecting to an *external* database.

# --- Other Variables ---
NODE_ENV=development # Set to 'production' in Render
PORT=3001 # Render injects its own PORT variable, this is for local
BASE_URL=http://localhost:3001 # Base URL for verification links etc. Update for Render

# --- Security Secrets (Generate strong random strings!) ---
SESSION_SECRET=your_strong_random_session_secret_here_123!@#
JWT_SECRET=your_other_strong_random_jwt_secret_456$%^

# --- Email Configuration ---
GMAIL_USER=hardikchaudhary713@gmail.com
GMAIL_APP_PASSWORD=atxj ijbl xuxt gwfe # Use App Password, keep secure!

# --- COMMENT OUT or REMOVE old MySQL vars ---
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=Hardik@12345
# DB_NAME=user_activity